#include "../LIB/cub3d.h"

int	render_frame (t_mlxy *mlx)
{
	int	x;
	int	y;
	int	color;

	mlx_clear_window(mlx->mlx, mlx->win);
	y = 0;
	while (y < mlx->map.height)
	{
		x = 0;
		while (x < mlx->map.width)
		{
			color = 
		}
	}

